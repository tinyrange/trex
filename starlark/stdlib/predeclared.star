"""Declares optional embedded extensions to predeclared Go namespaces."""

# Version policy is imported explicitly by recipes. Keeping this table empty
# prevents Starlark functions from masquerading as Go-backed builtins.
PREDECLARED = {}
