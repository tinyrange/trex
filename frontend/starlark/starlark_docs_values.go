package starlarkfrontend

// Overviews explain the ownership and operations of returned capability values.
var nativeStarlarkValueDescriptions = map[string]string{
	"windows.memory_image":  "A borrowed physical capture. read requires all bytes; probe returns data (the valid prefix), complete and fault. Fault kinds distinguish not-captured bytes from source-error failures. view returns a bounded, lazy read-only file for existing binary readers. Reads/views are limited to 64 MiB each. ranges returns an independent mapping list. address_space constructs an i386 paging view from an explicit CR3 and PAE mode without altering the source.",
	"windows.address_space": "An i386 virtual-memory view with explicit CR3 and PAE mode. read/probe/view share physical capture semantics and never substitute zeros for absent data. translate returns mapped, physical_address, page_size, raw entries and fault; a present translation does not guarantee its payload was captured. Faults identify the virtual address, physical address and paging stage. Supports present 4 KiB, non-PAE 4 MiB and PAE 2 MiB mappings; not-present software PTEs, PSE-36 and x64 paging are not recovered. No CPU permission validation is implied. Structure interpretation lives in @stdlib//inspect:memory.star.",
	"crypto.hasher":         "A mutable incremental digest. update feeds binary input, sum returns the current digest without clearing state, and reset discards accumulated input. Frozen hashers cannot be updated or reset.",
	"emulator.plugin":       "A named installation callback with explicit mutable state and optional caller-defined attributes. The emulator calls install(machine) when attaching the plugin; state makes checkpoint participation explicit instead of relying on closure-hidden mutations.",
	"emulator.machine":      "The AMD64 execution context returned by emulator.machine for a 64-bit target; x86 targets use the emulator.x86 value. Calls marshal Windows x64 integer arguments, while hooks and provided exports model APIs. imports_named filters import records and provide_exports binds an ordered name-to-argument-count table. Memory/register methods affect guest state; snapshot copies the context, but callback closure state is not independently forked. See [emulation](../emulation.md) for architecture-specific behavior.",
	"ar":                    "An ordered ar member inventory. entries/files expose member records and file views; find(name, occurrence=0) selects a particular occurrence when an archive repeats a member name.",
	"ar_entry":              "One ar member, combining ownership, mode and timestamp metadata with a file view of its payload. Byte reads and slices operate on this member, not on the enclosing archive.",
	"binary.builder":        "A mutable, bounded byte buffer. append and scalar methods grow it; reserve adds filled space, align adds padding, and patch methods replace existing ranges. bytes() materializes the result and file() exposes it as a file.",
	"binary.cursor":         "A sequential reader with an explicit byte offset. Scalar methods and bytes(size) advance the cursor; seek sets an absolute position, skip moves relatively, and align advances to an alignment boundary. remaining reports unread bytes.",
	"binary.layout":         "A fixed-size binary record layout. decode reads a record from a source offset; encode serializes supplied field values using the same layout. size is the encoded byte width.",
	"binary.xml_document":   "An immutable parsed XML document. root exposes its element tree; with_root returns a new document and bytes serializes it within an output bound.",
	"binary.xml_node":       "An immutable namespace-aware element. child/children_named select direct children and attribute performs a named lookup; direct_text excludes descendants while text includes their text. with_children and with_text return edited nodes without changing the original.",
	"block_device":          "A random-access block device with declared geometry and capabilities. read/write, zero and trim address byte ranges; extents reports range layout, flush requests durability, and snapshot/commit are available only where supported. stats exposes backend counters.",
	"byte_channel":          "An owned bidirectional byte transport. read requests a specified amount, read_some returns available bytes within its bound/timeout, write sends bytes, and close releases the endpoint. Channel operations do not imply any particular wire protocol.",
	"byte_view":             "A bounded random-access view of binary data. slice derives a subview, bytes reads a range, find returns a match position, find_all collects bounded matches, and find_indices searches several needles. compare performs the requested binary comparison without advancing a cursor.",
	"clock.profiler":        "An explicit instrumentation collector. span/end time a region, measure wraps a call and counter adds a named quantity. snapshot exposes current measurements and report applies the requested coverage criterion.",
	"clock.span":            "An open timed region belonging to a profiler. end() closes the region and returns its elapsed duration in seconds.",
	"compiledModule":        "A compilation result rather than an automatically executed program. Check ok and diagnostic before using binary; outputs maps virtual output names to immutable bytes.",
	"directory":             "A mutable in-memory directory tree. write adds file content, mkdir creates directories, find retrieves an entry and remove deletes one. Attributes and security descriptors are stored as image-construction metadata; fat_short_path reports the FAT-compatible short-name path.",
	"emulator.execution":    "A retained in-process execution that can be advanced with a bounded run. done distinguishes completed execution from a suspended one; close releases it and closed records that lifecycle state. It is not a QEMU snapshot.",
	"emulator.x86":          "A bounded 32-bit guest execution context. run/call/invoke execute modeled instructions; hooks and provided exports bind guest API calls to callbacks. Read/write and register methods inspect or change guest state. checkpoint/restore and snapshot support controlled variants; tracing, watches and profiles provide bounded observations. Acceleration/rewrite/transform operations require their documented code-identity checks; see [emulation](../emulation.md) for their contracts and checkpoint limitations.",
	"gdb":                   "A live remote-debugging session. continue, step, interrupt and wait change or observe target execution; register and memory operations use the selected thread/context. Breakpoints and watchpoints return removable handles. with_state/with_register temporarily modify target state around a callback; packet and monitor expose protocol-specific control.",
	"gdb_address_space":     "A checked page-table context for reading target virtual memory. page_table and kind identify the address space; generation ties it to the debugger's observed state so stale handles can be rejected.",
	"gdb_point":             "A breakpoint or watchpoint installed in a GDB target. remove deletes it and removed reports that state; with_disabled runs a callback while the point is temporarily disabled.",
	"installer":             "A recognized installer container with payload files and detection metadata. plan returns declarative modifications using caller-supplied locations, variables and component selection. It does not apply the plan or run a host installer.",
	"installscript":         "A parsed InstallShield script with functions, callbacks, calls and effects. find_function locates a declaration; evaluate interprets the supported subset using explicit state and step/depth bounds, returning analysis rather than running a host executable.",
	"installshield":         "An InstallShield cabinet view. Files, groups, components and shortcuts preserve package organization; find selects a member path. version identifies the parsed package generation.",
	"nbd_server":            "An NBD export bound to a block device. serve processes protocol requests on a supplied byte channel, stats exposes activity and close ends service. It does not own an implicit host listening socket.",
	"qemu_acpi_table":       "A launch descriptor carrying an ACPI table file and its QEMU properties. It is configuration data consumed by the backend, not a running guest object.",
	"qemu_chardev":          "A named QEMU character-backend descriptor. properties are passed through the backend's supported configuration handling when a VM is launched.",
	"qemu_device":           "A QEMU device-model descriptor with named properties. Adding the descriptor to a backend configuration selects guest hardware at launch.",
	"qemu_netdev":           "A QEMU network-backend descriptor. name identifies the backend kind and properties configure it; the object itself does not start networking.",
	"qemu_option":           "A validated QEMU option descriptor with its value/properties. It is interpreted by the backend at launch, not executed by a shell.",
	"qemu.v1":               "The backend-specific extension of a running QEMU VM. qmp sends structured monitor requests, qmp_schema inspects available commands, hmp uses the human monitor and block_stats reports storage activity. process exposes backend lifecycle state; these operations are not portable to every VMM.",
	"sfp":                   "A parsed SFP package inventory. entries/files expose member metadata and content, find selects a path, and package_label/version identify the package. archive_size/data_offset describe the enclosing container.",
	"sfp_entry":             "An SFP member with its logical path, entry type, timestamps and stored/unpacked lengths. Its read/bytes/slice operations expose decoded member content while payload_offset/record_offset locate its archive records.",
	"tar":                   "An ordered tar archive view. entries/files retain metadata and payload views; find(path, occurrence=0) selects repeated paths without losing earlier archive entries.",
	"tar_entry":             "A tar member with path, type, ownership, permissions, timestamp and link target. Payload reads are relative to the member; entry_type and link distinguish regular content from directories and links.",
	"vm":                    "A live virtual machine. Input helpers send guest keys, text or pointer events; screenshot captures the display. pause/resume/reset/powerdown/shutdown/stop control lifecycle, wait observes completion and next_event consumes events. channel and debugger open declared guest endpoints; extensions expose backend-specific features. Check capabilities before using optional operations.",
	"vmm_backend":           "A backend configuration and its advertised capabilities. id identifies the implementation; machine, accelerator and block_transport describe backend choices used during validation and startup.",
	"vmm_channel":           "A portable request for a named guest communication channel. kind selects its purpose and required controls whether unsupported channels make validation fail.",
	"vmm_disk":              "A guest storage attachment specification. device supplies bytes, bus/unit choose placement, media distinguishes disk from optical media, and read_only/snapshot specify write policy. CHS is optional legacy geometry.",
	"vmm_display":           "A guest display requirement. mode selects the requested display arrangement and required distinguishes a mandatory capability from an optional preference.",
	"vmm_machine":           "A portable VM specification, not a live VM. It records architecture, resources, disks, networks, channels, display and required capabilities for backend validation and vmm.start.",
	"vmm_network":           "A named portable guest-network request. kind selects networking behavior; required controls whether a backend may omit unsupported networking.",
	"windows.kd":            "A Windows kernel-debugging session over a byte channel. breakin/continue control execution, next_event observes notifications, context/set_context access registers and read/write methods access virtual or physical memory. request/packet expose lower-level protocol operations; file_io installs a handler for guest debugger file requests.",
	"windows.kd_breakpoint": "A breakpoint installed through the Windows KD protocol. handle identifies the remote breakpoint; remove deletes it and removed reports the local handle state.",
	"windows.pdb":           "A parsed PDB identity and symbol index. GUID/age or signature identify its build; symbols exposes entries and nearest(rva) finds the nearest applicable symbol for address annotation.",
	"windows.pdb_symbol":    "One PDB symbol record containing its name, kind and relative virtual address. It identifies metadata in the parsed module, not an automatically resolved live-process address.",
	"windows.pe":            "A lazy PE32/PE32+ inspection value. Metadata attributes expose headers, sections, imports, exports, resources, strings and debug/type-library data from an owned snapshot. read/disasm use RVAs; patch returns modified file bytes without changing the original. data provides the shared immutable source snapshot.",
}
